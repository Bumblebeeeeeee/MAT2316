#include <stdio.h>
#include <conio.h>
	int main(){
		int n;
		printf("neu la nam hay nhap 1 neu la nu hay nhap 0 :\n");
		scanf("%d", &n);
		if(n == 1) 
			printf("ban that dep trai !!!");
		else
			printf("ban that xinh gai !!!");
		getch();
		return 0;
	}
